// Same local-only defaults as environment.ts — see the comment there.
// Kept as a separate file only because the Angular CLI's `development`
// build configuration expects a matching environments/environment.development.ts
export { environment } from './environment';
