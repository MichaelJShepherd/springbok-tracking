# Secrets & local config — never committed (see AGENTS.md 1.2)
.env
.env.*
!.env.example
*.pem
*.key

# Local-only Claude state — never committed (#93): agent worktrees are live
# git checkouts, and skills are internal One Eleven process (AGENTS.md 1.1)
.claude/

# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/

# Node / Angular / ingestion build output (task #73)
node_modules/
dist/
.angular/
*.tsbuildinfo

# Supabase local temp files (task #73)
supabase/.temp/
supabase/.branches/
.env.local
